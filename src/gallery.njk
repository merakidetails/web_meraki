---
layout: layout.njk
title: Galería - Meraki Gift Store
---

<header class="relative py-20 overflow-hidden text-center">
    <div class="absolute inset-0 bg-gradient-to-br from-primary-light via-transparent to-transparent pointer-events-none -z-10"></div>
    <h1 class="text-4xl md:text-5xl font-extrabold mb-6">
        <span class="text-primary block text-lg font-bold uppercase tracking-widest mb-2">Nuestro Portafolio</span>
        Galería de trabajos anteriores
    </h1>
    <p class="text-lg text-gray-600 max-w-2xl mx-auto">Descubre la magia de los detalles hechos a mano. Cada pieza es única, creada con pasión.</p>
</header>

<section class="pb-24 px-4 sm:px-6 lg:px-8">
    <div class="max-w-7xl mx-auto">
<div x-data="{ selectedCat: 'Todos' }">
    <div class="flex flex-wrap justify-center gap-4 mb-12">
        <button @click="selectedCat = 'Todos'" 
                :class="selectedCat === 'Todos' ? 'bg-primary text-white border-primary' : 'bg-white dark:bg-surface-dark border-gray-200 dark:border-gray-700 text-gray-600 dark:text-gray-300 hover:border-primary hover:text-primary'"
                class="px-6 py-2 border rounded-full font-medium transition-all">Todos</button>
        
        {% set categories = ["Camisas", "Tazas", "Botellas", "Llaveros", "Cuadros", "Otros"] %}
        {% for cat in categories %}
        <button @click="selectedCat = '{{ cat }}'" 
                :class="selectedCat === '{{ cat }}' ? 'bg-primary text-white border-primary' : 'bg-white dark:bg-surface-dark border-gray-200 dark:border-gray-700 text-gray-600 dark:text-gray-300 hover:border-primary hover:text-primary'"
                class="px-6 py-2 border rounded-full font-medium transition-all">{{ cat }}</button>
        {% endfor %}
    </div>

    <div class="columns-1 md:columns-2 lg:columns-3 gap-6 space-y-6">
        {% for item in gallery %}
        <div x-show="selectedCat === 'Todos' || selectedCat === '{{ item.category }}'" 
             x-transition.opacity.duration.300ms
             class="break-inside-avoid group relative rounded-xl overflow-hidden shadow-sm hover:shadow-xl transition-all cursor-pointer bg-gray-100 dark:bg-gray-800">
            {% if item.image %}
            <img class="w-full h-auto object-cover group-hover:scale-105 transition-transform duration-500" 
                 src="{{ sanity.urlFor(item.image).width(600).url() }}" 
                 alt="{{ item.title }}" 
                 loading="lazy" />
            {% else %}
            <div class="w-full aspect-square flex items-center justify-center bg-gradient-to-br from-primary/20 to-primary/5">
                <div class="text-center p-6">
                    <span class="material-icons text-6xl text-primary/30 mb-2">image</span>
                    <p class="text-sm text-gray-500 dark:text-gray-400">Sin imagen</p>
                </div>
            </div>
            {% endif %}
            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity flex flex-col justify-end p-6">
                <span class="text-primary-light text-xs font-bold uppercase mb-1 tracking-wider">{{ item.category }}</span>
                <h3 class="text-white text-lg font-bold leading-tight">{{ item.title }}</h3>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
    </div>
    </div>
</section>
